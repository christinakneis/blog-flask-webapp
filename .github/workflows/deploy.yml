name: Deploy to EC2

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Deploy to EC2
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          cd blog-flask-webapp
          sudo git pull origin main
          
          # Kill existing gunicorn process
          sudo pkill gunicorn || true
          
          # Create/update virtual environment
          sudo python3 -m venv venv
          source venv/bin/activate
          
          # Install/update requirements
          sudo pip install -r requirements.txt
          
          # Start the application
          export APP_PORT=5000
          sudo nohup gunicorn -w 1 -b 0.0.0.0:$APP_PORT run:app > /var/log/gunicorn.log 2>&1 &
          
          # Wait a moment for the app to start
          sleep 5
          
          # Check if the app is running
          if pgrep gunicorn > /dev/null; then
            echo "✅ Deployment successful! Gunicorn is running."
          else
            echo "❌ Deployment failed! Gunicorn is not running."
            exit 1
          fi
